// this veriable is use store a quote
let quote: string =
  "A Person who never made a mistake never tried anything new";

// this veriable is use to store a personName
let famous_person = "Albert Einstein";

// this is use to store a message for print
let message = (`${famous_person} once said, ${quote}`);

// this is use for print all veriables in terminal
console.log(message);